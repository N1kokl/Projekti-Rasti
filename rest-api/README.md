# REST API (Express + MySQL)

This REST API provides CRUD operations for `book` and `car` tables.

## Setup

```bash
cp .env.example .env
npm install
npm start
```

## Database

Run:
- `sql/schema.sql`

